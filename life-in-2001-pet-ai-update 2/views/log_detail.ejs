<!DOCTYPE html>
<html>
...
</html>
<!DOCTYPE html>
<html>
<head>
  <title>Flagged Log</title>
  <style>
    body { font-family: sans-serif; background: #111; color: white; padding: 20px; }
    .log { background: #222; padding: 15px; border-left: 5px solid #f55; margin-bottom: 20px; }
    form { margin-top: 20px; }
    input { padding: 10px; background: #222; color: white; border: none; margin: 10px 0; width: 100%; }
    button { background: #f55; color: white; padding: 8px 16px; border: none; width: 100%; }
  </style>
</head>
<body>
  <h1>📜 Chat Log</h1>
  <div class="log">
    <p><strong>User:</strong> <%= log.user %></p>
    <p><strong>Time:</strong> <%= log.time %></p>
    <p><strong>Flagged Message:</strong> <%= log.message %></p>
    <hr />
    <% if (log.fullLog) { log.fullLog.forEach(msg => { %>
      <p><%= msg %></p>
    <% }) } %>
  </div>
  <form method="POST" action="/ban/<%= index %>">
    <label>Ban duration (minutes):</label><br>
    <input type="number" name="duration" value="10" required />
    <button type="submit">Ban User</button>
  </form>
</body>
</html>
