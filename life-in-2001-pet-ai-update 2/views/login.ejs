<!DOCTYPE html>
<html>
...
</html>
<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <title>Login | AI Moderator</title>
  <style>
    body { font-family: sans-serif; background: #111; color: white; display: flex; align-items: center; justify-content: center; height: 100vh; }
    form { background: #222; padding: 20px; border-radius: 8px; width: 300px; }
    input { width: 100%; padding: 10px; margin: 10px 0; background: #333; border: none; color: white; }
    button { width: 100%; background: #0f7; color: black; padding: 10px; border: none; font-weight: bold; }
    .error { color: red; }
  </style>
</head>
<body>
  <form method="POST" action="/login">
    <h2>🔒 Moderator Login</h2>
    <% if (error) { %><p class="error"><%= error %></p><% } %>
    <input type="text" name="username" placeholder="Username" required />
    <input type="password" name="password" placeholder="Password" required />
    <button type="submit">Login</button>
  </form>
</body>
</html>
