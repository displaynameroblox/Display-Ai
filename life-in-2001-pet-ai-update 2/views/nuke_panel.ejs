<!DOCTYPE html>
<html>
<head>
  <title>Owner Nuke Control</title>
  <style>
    body {
      background: #000;
      color: white;
      font-family: 'Segoe UI', sans-serif;
      text-align: center;
      padding: 50px;
    }

    h1 {
      color: #ff4c4c;
      font-size: 3em;
      margin-bottom: 30px;
    }

    .nuke-btn {
      background: linear-gradient(145deg, #f00, #a00);
      border: none;
      color: white;
      padding: 14px 28px;
      font-size: 18px;
      border-radius: 8px;
      margin: 10px;
      cursor: pointer;
      transition: 0.2s ease;
      box-shadow: 0 0 10px #f00;
    }

    .nuke-btn:hover {
      background: linear-gradient(145deg, #ff2222, #880000);
      box-shadow: 0 0 25px #ff4444;
    }

    .sun {
      margin-top: 50px;
      position: relative;
      width: 300px;
      height: 300px;
      margin-left: auto;
      margin-right: auto;
      background: radial-gradient(circle, #ffff88 0%, #ff6600 70%, #ff2200 100%);
      border-radius: 50%;
      box-shadow: 0 0 100px 40px #ff6600;
      animation: pulse 2s infinite;
    }

    .sun img {
      width: 200px;
      margin-top: 50px;
      border-radius: 12px;
    }

    @keyframes pulse {
      0% { transform: scale(1); box-shadow: 0 0 100px 40px #ff6600; }
      50% { transform: scale(1.05); box-shadow: 0 0 140px 60px #ff3300; }
      100% { transform: scale(1); box-shadow: 0 0 100px 40px #ff6600; }
    }
  </style>
</head>
<body>
  <h1>☢️ Nuke Control Center</h1>
  <form method="POST" action="/nuke/logs"><button class="nuke-btn">💣 Wipe Chat Logs</button></form>
  <form method="POST" action="/nuke/memory"><button class="nuke-btn">🧠 Clear AI Memory</button></form>
  <form method="POST" action="/nuke/all"><button class="nuke-btn">🌍 Total System Wipe</button></form>

  <div class="sun">
    <img src="https://media.tenor.com/j9nXYgdfEVEAAAAC/lebron-james-you-are-my-sunshine.gif" alt="Lebron James Sunshine Meme" />
  </div>
</body>
</html>
<a href="/nuke" class="nuke-button">☢️ Nuke All</a>
