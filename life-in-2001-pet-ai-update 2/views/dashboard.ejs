      <!DOCTYPE html>
      <html>
      <head>
        <title>Moderator Panel</title>
        <style>
          body {
            font-family: sans-serif;
            background: #111;
            color: white;
            padding: 20px;
          }

          .entry {
            background: #222;
            padding: 12px;
            margin: 10px 0;
            border-left: 5px solid #f55;
          }

          .entry p {
            margin: 5px 0;
          }

          button, a.btn {
            background: #f55;
            color: white;
            padding: 6px 12px;
            border: none;
            border-radius: 4px;
            text-decoration: none;
            display: inline-block;
            margin-right: 8px;
          }

          .nav {
            margin-bottom: 20px;
          }

          .nuke-button {
            display: inline-block;
            background: linear-gradient(135deg, #ff4444, #990000);
            color: #fff;
            font-weight: bold;
            padding: 12px 20px;
            border-radius: 10px;
            text-decoration: none;
            transition: 0.3s ease;
            box-shadow: 0 0 10px #ff4d4d;
            margin-top: 30px;
          }

          .nuke-button:hover {
            background: linear-gradient(135deg, #ff2222, #660000);
            box-shadow: 0 0 20px #ff1a1a;
          }

        </style>
      </head>
      <body>
        <div class="nav">
          <form method="POST" action="/logout" style="display:inline;">
            <button>🚪 Logout</button>
          </form>
          <a href="/settings" class="btn">⚙️ Settings</a>
        </div>

        <h1>🚨 Flagged Messages</h1>

        <% if (logs.length === 0) { %>
          <p>No flagged messages found.</p>
        <% } else { %>
          <% logs.forEach((log, i) => { %>
            <div class="entry">
              <p><strong>User:</strong> <%= log.user %></p>
              <p><strong>Message:</strong> <%= log.message %></p>
              <a class="btn" href="/flag/<%= i %>">View Full Log</a>
            </div>
          <% }) %>
        <% } %>

        <% if (isOwner) { %>
          <div style="text-align: center;">
            <a href="/nuke" class="nuke-button" onclick="return confirm('⚠️ Are you sure you want to NUKE the system? This will wipe all AI data!')">
              💥 NUKE SYSTEM
            </a>
          </div>
        <% } %>
      </body>
      </html>
